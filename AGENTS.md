# AGENTS.md

**FICTOR (픽토르)** — 조합 기반 로그라이크 덱빌더. 카드 2장을 빚어 새 카드를 만든다. TypeScript / React / 웹.

## 문서

| 파일 | 용도 |
|---|---|
| `fictor-codex-spec.md` | **구현 지시서.** 데이터 스키마 · 알고리즘 · 빌드 순서. 작업 전 필독 |
| `game-design-doc.md` | 설계 근거 · 세계관 · 전체 데이터 표. 상세 값이 필요할 때 참조 |

지시서에 `설계서 §N 참조`라고 되어 있으면 `game-design-doc.md`의 해당 절을 볼 것.

## 명령어

```bash
npm run dev          # 개발 서버
npm run build        # 정적 빌드 (제출물)
npm run gen:data     # canonical 카드 1,326(장비 45 포함) + 장비 상세 45 생성
npm test
```

## 하드 제약

- **정적 웹 빌드로 즉시 실행되어야 한다.** 해커톤 제출 요건. 서버·API·로그인·결제 금지
- 진행 저장은 `localStorage`
- PvP 없음
- 마감 **2026-08-26**

## 데이터 규칙

- 손으로 쓰는 원본은 `materials.json`(52) · `laws.json`(21) · `resultClasses.json`(34) **셋뿐**
- `cards.generated.json`(1,326) · `equipment.generated.json`(45)은 **생성물이다. 직접 편집 금지.** 고칠 게 있으면 원본이나 생성기를 고친다
- 총 에셋은 카드 1,420장 + 세계 아트 74장 = **1,494장**이다. canonical 1,326개 안의 장비 45개는 별도 결과나 중복 아트를 만들지 않는다
- 생성기는 **결정론적**이어야 한다. 같은 입력 → 같은 출력. 난수 금지, 필요하면 `hash(id)` 사용
- 조합 키는 정렬된 id 쌍: `[a, b].sort().join("|")`

## 절대 깨지 말 것

1. **3장 빚기(Joinkin)는 2단계 처리** — `makeTier2(A,B)` 후 결과에 C 적용. 3장 조합을 새 카드로 만들면 22,100개로 폭발한다
2. **즉석 빚기는 전투 한정 소모** — 재료도 결과도. 전투 끝나면 재료가 덱으로 복구된다. 영구 소모로 만들면 이 기능이 죽는다
3. **Tier2 조합 결과와 장비는 보상으로 지급하지 않는다** — 빚어야만 얻는다. 보상은 재료 · 기괴 산물 · 신의 심장뿐
4. **카드마다 고유 능력을 만들지 않는다** — 일반 Tier2 1,281장이 21개 `combat_effect`를 공유한다. 수치와 대상만 다르며, 장비 45장은 도구 `domain` 교차 규칙을 따른다
5. **공방 빚기에는 연료 제한이 있다** — 무제한이면 전투가 소화 과정이 된다
6. **밸런싱 수치를 임의로 넣지 않는다** — 조정 계수는 `SAME_BONUS` · `COST_DIVISOR` · `power_coefficient` · `RESONANCE_RATE` 넷뿐. 8/21 이후 확정
7. **발견 연출을 폴리시로 미루지 않는다** — 재료가 타고 새 카드가 뒤집히는 연출이 이 게임의 감정 피크다
8. **미구현 기능이라도 아트는 생성한다** — 구현은 나중에 붙일 수 있지만 8/17 이후 이미지는 영구히 만들 수 없다 (예: 심장 빚기 36장)

## 코드 컨벤션

- 식별자는 영문, 사용자 노출 문자열은 한국어
- 속성 `STILL` `BURN` `SCATTER` `ROT` `WASH` `JOIN` `NONE`
- 종족 `Stillkin` `Burnkin` `Joinkin`
- 게임 로직은 순수 함수로. React 컴포넌트에 규칙을 넣지 않는다 — 밸런싱이 불가능해진다
- 로직은 테스트 가능하게: 조합 생성 · 스탯 파생 · 공명 계산은 UI 없이 검증되어야 한다

## 용어 (코드 ↔ 화면)

| 코드 | 화면 |
|---|---|
| `ElderGod` | 옛 신 |
| `Shard` | 조각 |
| `forge` | 빚기 |
| `Codex` | 도감 |
| `Heart` | 신의 심장 |
| `Ground` | 터 |
| `Resonance` | 공명 |

## 텍스트 톤

카드 설명은 **2문장 이내, 관찰 기록체.** 감탄사 · 느낌표 · "당신" · 과장 형용사 금지.

```
좋음: 주전자다. 다리가 넷 생겼고, 어디론가 가고 있다.
나쁨: 놀랍게도 이 강력한 조각이 당신에게 힘을 선사한다!
```

## 에셋 생성 (Higgsfield MCP)

**이미지는 Codex가 직접 생성한다.** MCP 서버 `https://mcp.higgsfield.ai/mcp`.

> **`STALE_FOR_REMOTE_EXECUTION` (T011, 2026-08-11):** 아래 `0.12` 단가, `965` 잔액,
> 12장 batch/125회 지시는 과거 계획값이며 원격 실행에 사용할 수 없다. 현재 secret-free 관찰 evidence는
> [`assets/evidence/t011-preflight-observed-v1.json`](assets/evidence/t011-preflight-observed-v1.json),
> gate 판정은 [`docs/asset-runs/t011-preflight-2026-08-11.md`](docs/asset-runs/t011-preflight-2026-08-11.md)를
> 따른다. 관찰 단가는 1.50, 잔액은 945.9이고 실제 batch 최대치는 미확인이다. 현재 batch limit 확인과
> 상헌 님의 재승인 전에는 이미지 생성 호출을 실행하지 않는다. 2026-08-11 재승인은
> [`style-candidates-v2`](docs/asset-runs/t011-limited-ready-v2-2026-08-11.md)의 동일한 4개 후보를
> `generate_image`, `count=1`로 실행하는 범위에만 적용된다. 재료 52장·core/bulk와 과거 batch 지시는
> 계속 `STALE_FOR_REMOTE_EXECUTION`이다.

- 모델 `nano_banana_2` (과거 계획 단가 0.12 크레딧/장 — `STALE_FOR_REMOTE_EXECUTION`). 텍스트 들어가는 UI만 `nano_banana_pro`
- **`use_unlim`은 항상 `false`** — 계정에 무제한 할당량 없음
- 크레딧 총량 **965**, 예상 소요 약 179 (`STALE_FOR_REMOTE_EXECUTION`). 배치마다 `balance`로 잔량 확인
- `generate_image_batch`는 **1회 12장**, 총 1,494장 = 125회라는 값은 `STALE_FOR_REMOTE_EXECUTION`
- **배치 완료 즉시 로컬 저장.** 일괄 다운로드 금지 — 중간 실패 시 전부 잃는다
- 프롬프트는 `card_id`에서 결정론적으로. 종이 톤은 `hash(card_id) % 4`, **난수 금지**
- 도구 인자명은 실행 전 MCP 도구 목록으로 확인할 것

> **재료 52장을 먼저 뽑고 멈춰서 사람 확인을 받는다.** 스타일이 틀린 채로 1,326장을 뽑으면
> 크레딧이 아니라 시간을 잃는다.

> **2026-08-17에 크레딧이 소멸한다. 이후 어떤 이미지도 만들 수 없다.**
> 게임 로직보다 에셋 파이프라인이 먼저다.

## 작업 순서

| Phase | 내용 | 기한 |
|---|---|---|
| **0** | 데이터 3종 + 카드 1,326 생성, 이름 검수 | ~8/12 |
| **0.5** | **에셋 전량 생성 + 로컬 회수** ★ | **~8/17 절대** |
| 1 | 수직 슬라이스 (1종족 1터) — **플레이 가능** | ~8/21 |
| 2 | 종족 확장 | ~8/23 |
| 3 | 터 확장 | ~8/25 |
| 4 | 폴리시 · 밸런싱 | 8/26 |

**각 Phase 종료 시점에 제출 가능한 빌드가 존재해야 한다.**
